spring:
  datasource:
    url: jdbc:h2:mem:gymbook;MODE=MySQL
  h2:
    console:
      enabled: true
      path: /h2-console
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
