spring:
  application:
    name: lv2-final-mission

  flyway:
    enabled: true

  sql:
    init:
      mode: never

  jpa:
    open-in-view: false

api:
  get-holiday:
    base-url: ${HOLIDAY_API_URL}
    service-key: ${HOLIDAY_API_KEY}
